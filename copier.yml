
# questions
author:
    type: str
    help: What is your project name?
    default: MAPP Metabolomics Unit
    required: true

email:
    type: str
    help: What is your email?
    default: pierre-marie.allard@unifr.ch
    required: true

repository_name:
    type: str # Any value will be treated raw as a string
    help: The name of the repository on GitHub. should be lowercase and dash-separated
    default: johny-watanabe-group
    validator: >-
        {% if not (repository_name | regex_search('^[a-z][a-z0-9\-]+$')) %}
        repository_name must start with a letter, followed one or more letters, digits or dashes all lowercase.
        {% endif %}

module_name:
    type: str
    help: What is your Python module name?